---
name: frontend-polish
appliesTo: root, coding, review, plan_reviewer
triggers: UI, UX, webview, markdown, css, frontend, style, layout, 样式, 界面, 前端, 布局
---

# Frontend Polish Skill

For UI work:

- keep text from overflowing or overlapping
- keep activity/status rows compact and stable
- preserve VS Code theme variables
- render assistant Markdown safely
- keep user messages plain text
- verify both streaming and final states when message rendering changes
